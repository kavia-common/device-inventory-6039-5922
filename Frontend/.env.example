# PUBLIC_INTERFACE
# Vite-style and CRA both included for flexibility. This app uses CRA; prefer REACT_APP_API_BASE_URL.
# For Create React App:
REACT_APP_API_BASE_URL=http://localhost:5000

# For Vite (if migrated in the future):
VITE_API_BASE_URL=http://localhost:5000
